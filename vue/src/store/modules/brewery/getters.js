export const GET_BREWERY = (state) => {
  return state.brewery;
};
export const GET_BREWERIES = (state) => {
  return state.breweries;
};

export const GET_LIKED_BREWERIES = (state) => {
  return state.likedBreweries;
};

export const GET_API_BREWERIES = (state) => {
  return state.apiBreweries;
};

export const GET_API_BREWERY = (state) => {
  return state.apiBrewery;
};
